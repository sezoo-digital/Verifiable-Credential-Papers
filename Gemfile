source "https://rubygems.org"

# This emulates the exact software environment GitHub Pages uses
gem "github-pages", group: :jekyll_plugins

# This explicitly installs your new theme for local testing
gem "just-the-docs"